TODO
- VScode settings
- Rider settings
- make clear what parts couldnt be installed by main manager (brew) and create command listing that
- key manager?
- auto script for obsidian

## How to run
make all files allowed to run

## Extra's
https://www.withmarko.com/one-menu

Fingerprint scanner for mac
```
sed -e 's/^#auth/auth/' /etc/pam.d/sudo_local.template | sudo tee /etc/pam.d/sudo_local 
```