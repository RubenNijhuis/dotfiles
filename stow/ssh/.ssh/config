# SSH Configuration
# Uses modular includes for better organization

# OrbStack integration (must be at the top)
Include ~/.orbstack/ssh/config

# Common security defaults
Include ~/.ssh/config.d/common.conf

# Profile-specific configurations
Include ~/.ssh/config.d/personal.conf
Include ~/.ssh/config.d/work.conf

# Local overrides (gitignored)
Include ~/.ssh/config.d/local.conf
