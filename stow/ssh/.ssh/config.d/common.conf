# Common SSH security settings
Host *
    # macOS Keychain integration
    AddKeysToAgent yes
    UseKeychain yes
    IdentitiesOnly yes

    # Connection settings
    ServerAliveInterval 60
    ServerAliveCountMax 3
    TCPKeepAlive yes

    # Prevent SSH from trying all keys
    IdentityFile none
