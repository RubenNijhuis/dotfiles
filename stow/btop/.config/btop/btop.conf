# btop configuration — only non-default values

# Tokyo Night with distinct accent colors per graph
color_theme = "tokyo-night-custom"

# Use terminal background for transparency support
theme_background = False

# Show CPU temperatures (macOS: requires sudo or powermetrics)
shown_boxes = "cpu mem net proc"
