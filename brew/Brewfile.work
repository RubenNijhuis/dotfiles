# =============================================================================
# Work Brewfile - Work machine packages only
# =============================================================================
# Packages specific to work projects and clients
# Common packages shared with personal go in Brewfile.cli, Brewfile.apps, Brewfile.vscode

# Note: mycli and pgcli not currently in Brewfile but could be added if needed
# brew "mycli"   # MySQL CLI with autocomplete
# brew "pgcli"   # PostgreSQL CLI with autocomplete
